.navigation_buttons
  -permitted_to? :create, :vendors do
    -#=link_to new_vendor_path, remote: true do
    .new_image_link#new
      %li.icon-plus.icon-2x

.vendor_box
  %h2
    Vendors
  .search_bar
    %span.searching_bar.hidden
      Searching...
    -#=form_for :vendor, html: { class: 'vendor_search' }, method: :get do |f|
    -#  =f.text_field :search, placeholder: "Filter Vendors by Name", name: 'search'
    -#  =submit_tag "Search", class: 'hidden'
  .filter_bar
    %span{ bind: { class: ':letter isNoLetter:active'}, _action: 'letterClick' }
      ='All'
    =hb 'each getAllLetters' do
      =hb "view App.LetterPaginatorView contentBinding='this'"

    =hb 'view App.PaginatorView'

  .vendor_list
    =hb 'each' do
      =hb 'partial "vendor"'
