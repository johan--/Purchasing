.tabs
  -@tabs.each do |tab|
    -if @tab == tab
      .tab.clicked{ 'data-tab' => tab } #{tab}
    -else
      .tab{ 'data-tab' => tab } #{tab}
  .right_controls
    %span.buyers 
      Filter by Buyer:
      =select_tag "filter_by_user", options_for_select([["All", 0]] + @buyers, selected: @buyer)
    %span.pages
      ="Page #{@page} of #{@purchases.num_pages}"
      .purchase_pagination
        =paginate @purchases, remote: true
.header_fields
  .controls_header
  .date_cell_header
    Date
  .vendor_cell_header
    Vendor
  .requester_cell_header
    Requester
  .department_cell_header
    Department
  .buyer_cell_header
    Buyer
  .tag_cells_header
    Tags