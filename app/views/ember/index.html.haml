!!!
%html
  %head
    %title
      Purchasing
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1' }
    =stylesheet_link_tag 'application', media: "all"
    %script
      buyers = #{@buyers}; // Hacky way to get data from server without AJAX
    =javascript_include_tag 'application'
    =csrf_meta_tags
  %body
  -Dir["./app/views/templates/**/*.*"].each do |s|
    -filename = s.gsub(/^.*templates/,'').gsub(/\/_/, '/').gsub(/^\//, '')
    -puts filename
    %script{ type: 'text/x-handlebars', 'data-template-name' => filename.split('.')[0] }
      =render "templates/#{filename}"
