
.edit_title
  Tags

.tags
  =hb 'each tag in controller' do
    =hb 'view App.TagView'

  -if permitted_to? :create, :tags
    .add_tag_list{ title: 'Add a tag' }
      =hb 'view App.TagSelect content=tagsList'
