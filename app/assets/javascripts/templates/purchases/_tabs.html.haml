
%ul.nav.nav-tabs.purchases_tabs
  -@tabs.each do |tab|
    %li{ title: "View #{tab} records", bind: { class: "canTab#{tab}:active" } }
      %a{ href: '#', _action: "tabClick '#{tab}'" }
        =tab
