
-if permitted_to? :reconcile, :purchases

  =hb 'if isReconciling' do
    =hb 'if itemsSelected' do
      .action_button.green_back{ _action: 'unreconcileSelected', id: 'unreconcileSelected', title: 'Unreconcile selected records' }
        .total=hb 'numSelected'

    .action_button.blue{ _action: 'selectAll', title: 'Select all records on current page' }
      =fa_icon 'check'

    .action_button.yellow{ _action: 'selectNone', title: 'Unselect all records on current page' }
      =fa_icon 'ban'

    .action_button.red{ _action: 'stopActivities', title: 'Exit unreconciling mode' }
      =fa_icon 'times'
