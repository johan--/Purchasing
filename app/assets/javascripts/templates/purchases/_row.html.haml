%td
  -if permitted_to? :update, :purchases
    =hb 'partial "views/star"'
  -else
    =hb 'partial "views/star_noaction"'

%td
  =hb 'buyerInitials'
%td.text-center
  =hb 'medDate dateRequested'
%td
  %strong
    =hb 'vendorString'
%td
  =hb 'requester.name'
%td
  =hb 'requester.department'

%td.tag_cells
  =hb 'each tags' do
    .small_tag
      =hb 'name'

%td
  %strong
    =hb 'currency grandTotal'

%td
  .btn-group
    .btn.btn-sm.btn-default{ _action: 'openRecordShow target="view" bubbles=false' }
      Show
    .btn.btn-sm.btn-default.dropdown-toggle{ 'data-toggle' => 'dropdown' }
      %span.caret
      %span.sr-only Actions
    %ul.dropdown-menu{ 'role' => 'menu' }
      %li{ _action: 'openRecordShow target="view" bubbles=false', title: 'Show record' }
        %a{ href: '#' }
          =fa_icon 'coffee-o fw'
          Show
      %li{ _action: 'openRecordEdit target="view" bubbles=false', title: 'Edit record' }
        %a{ href: '#' }
          =fa_icon 'edit fw'
          Edit
      -permitted_to? :delete, :purchases do
        %li{ _action: 'deleteMe target="view" bubbles=false', title: 'Delete record' }
          %a{ href: '#' }
            =fa_icon 'times fw'
            Delete
